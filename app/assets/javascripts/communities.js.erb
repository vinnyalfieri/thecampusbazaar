alert("hello!");

$(document).ready(function(){
  // var items = $(".itembox")
    alert("hello!");

    $('#filters :checkbox').click(function () {
        if ($('input:checkbox:checked').length) {
            $('.itembox').hide();
            $('input:checkbox:checked').each(function () {
                $('.itembox[data-price' + '*="' + $(this).val() + '"]').show();
            });
        } else {
            $(".itembox").show();
        }
    });
});
