<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
        <i class="fa fa-bars"></i>
      </button>
      <%= link_to 'CampusBazaar', root_path, {class: "navbar-brand page-scroll"} %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
      <ul class="nav navbar-nav">

        <!-- Adds Sell a New Item Link to NAVBAR ONLY when a user is logged in. -->
        <% if current_user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Your Account <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to  "Edit Your Account", user_path(current_user) %></li>
            <li role="separator" class="divider"></li>
            <li ><%= link_to("Messages #{current_user.mailbox.inbox(:unread => true).count(:id, :distinct => true)}", conversations_path) %></li>
            <li role="separator" class="divider"></li>
            <li ><%= link_to('Sell an Item', new_user_item_path(:user_id => current_user.id)) %> </li>
            <li role="separator" class="divider"></li>
            <li ><%= link_to('Manage Your Items', user_items_path(:user_id => current_user.id)) %> </li>
            <li role="separator" class="divider"></li>
            <% if current_user.seller? %>
            <li class="dropdown dropdown-submenu"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Offers</a>
                <ul class="dropdown-menu">
                <li><%= link_to('Offers you made', offers_sent_path(current_user))%></li>
                <li><%= link_to('Offers for your items', offers_received_path(current_user))%></li>
              </ul>
             </li>
             <% else %>
            <li ><%= link_to('Offers Sent', offers_sent_path(current_user)) %> </li>
             <% end %>
          </ul>
          <% end %>
        </li>
        <li class="hidden">
          <a href="#page-top"></a> 
        </li>
        <% if current_user %>
        <li><p class='navbar-text'><%= "Signed in as #{current_user.name}" %></p></li>
        <li><p class='navbar-text'>|</p></li>
        <li><%= link_to "Logout", '/logout' %></li>
        <% else %>
        <li><%= link_to 'Login', '/login' %></li>
        <li><p class='navbar-text'>|</p></li>
        <li><%= link_to 'Signup', '/signup' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
