<td><%= link_to offer.seller.name, user_path(offer.seller) %></td>
<td><%= link_to offer.item.name, user_item_path(offer.seller,offer.item) %></td>
<td><%= offer.item.price %></td>
<td><%= offer.offer_price %></td>
<td><% if offer.comment %>
  <%= offer.comment %>
<% end %></td>
<td><%= time_ago_in_words(offer.created_at) %></td>

<td> <% if offer.status == 'pending' %>
<%= link_to "Cancel", offer_path(offer), {:method => 'delete', :class => 'btn btn-danger btn-xs' } %>
<% end %> </td>
